(() => {
  const defaults = Object.freeze({
    markerMode: "dot",
    color: "#d62828",
    opacity: 0.9,
    strokeWidth: 2,
    dotSize: 14,
  });

  globalThis.CENTERLINE_DEFAULTS = defaults;
})();
